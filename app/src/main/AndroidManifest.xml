<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.spotxchange.spotxadtest" >

    <!-- REQUIRED -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <!-- REQUIRED -->
        <meta-data android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".MainActivity"
            android:configChanges="keyboard|orientation|screenSize|keyboardHidden"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.spotxchange.integration.mraid.SpotXInterstitialActivity"
            android:configChanges="keyboard|orientation|screenSize"
            android:label="SpotXInterstitial" >
        </activity>

        <!-- Necessary for the custom inline browser -->
        <activity android:name="com.spotxchange.integration.mraid.SpotXMraidBrowser" />

    </application>

</manifest>
